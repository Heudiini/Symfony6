

{# This template uses inherit method that inherits all the data variables that comes from the parent , here it is index.html.twig e.g messages,also message as its inside the for loop, and limit variable, #}

<div style="color: grey;">
{% if date(message.created) < date('-1year') %}
  Older than 1 year
{% else %}
  {{ message.created|date('d/m/Y') }}
{% endif %}
</div>